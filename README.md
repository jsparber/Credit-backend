# Credit-backend
Javascript backend for the Credit app

## Run
Clone the repo and
create a file named loginData.js with:
    module.exports = function() {
      var data = {};
      data.user = "";
      data.password = "";
      return data;
    }
